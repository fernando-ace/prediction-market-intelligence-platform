{
  "name": "@prediction-market-scanner/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "worker": "tsx src/index.ts",
    "collect:sample": "tsx src/collect-sample.ts",
    "report:overnight": "tsx src/report-overnight.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@prediction-market-scanner/core": "0.1.0",
    "@prediction-market-scanner/db": "0.1.0",
    "dotenv": "^16.5.0",
    "tsx": "^4.19.4"
  },
  "devDependencies": {
    "vitest": "^3.2.2"
  }
}
